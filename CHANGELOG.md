# Changelog

## Unreleased

### Added

- Added `use-material-elasticity` as the materials-owned elasticity package.

### Changed

- Renamed the materials elasticity crate from `use-elasticity` / `use_elasticity` to
	`use-material-elasticity` / `use_material_elasticity`, leaving the `use-elasticity` package name
	available for the physics crate line.
